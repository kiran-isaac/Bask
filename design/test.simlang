alias String [char]

struct Num with (
  String name, 
  int val
)

enum NumEnum with int {
  One(1),
  Two(2),
  Three(3)
}

fn main(int argc, [String] argv) -> void {
  for (int i = 0; i < argc; i++) {
    puts(argv[i])
  }
}